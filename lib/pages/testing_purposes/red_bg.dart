import 'package:flutter/material.dart';

class red_bg extends StatelessWidget {
  const red_bg({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
    );
  }
}